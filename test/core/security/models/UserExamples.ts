import { TestExamples } from '../../common/base/testing/TestExamples'

export const usernames = new TestExamples(['admin', 'alice', 'bob'])

export const admin = {
    username: 'admin',
    password: '1234',
    sessionToken: 'admin_session_token',
}
