export enum Roles {
    Member = 'Member',
    Administrative = 'Administrative',
    ProductOwner = 'ProductOwner',
    Admin = 'Admin',
}

export const allRoles = [
    Roles.Admin,
    Roles.Member,
    Roles.Administrative,
    Roles.ProductOwner,
]